import React, { useState } from 'react';
import Card from '@/components/common/Card';
import Button from '@/components/common/Button';
import { 
  FileText, 
  Download, 
  Printer, 
  FileSpreadsheet, 
  Filter, 
  CheckCircle,
  Building,
  Calendar,
  Layers
} from 'lucide-react';

const EvaluationReports = () => {
  const [selectedFormat, setSelectedFormat] = useState('PDF');
  const [selectedScope, setSelectedScope] = useState('Entire Database');

  const reportCategories = [
    { title: 'Student Evaluation Report', code: 'REP-STD', desc: 'Detailed individual student marks, rubrics, and feedback summary.' },
    { title: 'Team Evaluation Report', code: 'REP-TEAM', desc: 'Comprehensive team-wise consolidated assessment matrix.' },
    { title: 'Guide Assessment Summary', code: 'REP-GDE', desc: 'Internal guide marks and milestone completion log.' },
    { title: 'Reviewer Panel Defense Report', code: 'REP-REV', desc: 'External reviewer panel scores for Review 1, Review 2, and Review 3.' },
    { title: 'Faculty Internal Assessment', code: 'REP-FAC', desc: 'Classroom faculty internal viva and implementation marks.' },
    { title: 'Department Performance Summary', code: 'REP-DPT', desc: 'Department-wide grade distribution and completion metrics.' },
    { title: 'Final Consolidated Marks Sheet', code: 'REP-FNL', desc: 'Official weighted score final marks sheet with grades and pass/fail status.' },
    { title: 'Audit Trail & Version History Report', code: 'REP-AUD', desc: 'Complete log of score modifications, stage changes, and locking events.' }
  ];

  const handleGenerateReport = (title) => {
    alert(`Generating official ${title} in ${selectedFormat} format (${selectedScope}) with KL CSE Capstone Portal header.`);
  };

  return (
    <div className="space-y-6">
      
      {/* Report Controls & Format Selection */}
      <Card className="p-6 bg-gradient-to-r from-slate-900 via-slate-800 to-slate-900 text-white shadow-xl">
        <div className="flex flex-col md:flex-row md:items-center justify-between gap-6">
          <div>
            <div className="flex items-center gap-2">
              <span className="px-2.5 py-1 text-xs font-bold bg-primary-500/20 text-primary-300 rounded border border-primary-500/30">
                Official ERP Report Generator
              </span>
            </div>
            <h2 className="text-2xl font-bold mt-2 tracking-tight">University Reports Center</h2>
            <p className="text-slate-300 text-xs mt-1">
              Includes University Logo, Department, Academic Year, Generated By/On, and Watermark.
            </p>
          </div>

          <div className="flex flex-wrap items-center gap-4">
            <div>
              <label className="block text-[11px] font-bold text-slate-300 uppercase mb-1">Export Format</label>
              <select
                value={selectedFormat}
                onChange={(e) => setSelectedFormat(e.target.value)}
                className="text-xs font-bold bg-slate-800 border-slate-700 text-white rounded-lg p-2 focus:ring-primary-500"
              >
                <option value="PDF">PDF Document</option>
                <option value="Excel">Excel (.xlsx)</option>
                <option value="CSV">CSV Spreadsheet</option>
                <option value="Print">Print Preview</option>
              </select>
            </div>

            <div>
              <label className="block text-[11px] font-bold text-slate-300 uppercase mb-1">Target Scope</label>
              <select
                value={selectedScope}
                onChange={(e) => setSelectedScope(e.target.value)}
                className="text-xs font-bold bg-slate-800 border-slate-700 text-white rounded-lg p-2 focus:ring-primary-500"
              >
                <option value="Entire Database">Entire Database</option>
                <option value="Current View">Current View</option>
                <option value="Filtered Data">Filtered Data</option>
                <option value="Selected Teams">Selected Teams</option>
              </select>
            </div>
          </div>
        </div>
      </Card>

      {/* Available Reports Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        {reportCategories.map((r) => (
          <Card key={r.code} className="p-5 flex flex-col justify-between hover:border-primary-400 transition-all">
            <div>
              <div className="flex items-center justify-between">
                <span className="font-extrabold text-xs text-primary-700 bg-primary-50 px-2 py-0.5 rounded border border-primary-100">{r.code}</span>
                <span className="text-[10px] text-gray-400 font-medium">Format: {selectedFormat}</span>
              </div>
              <h4 className="font-bold text-gray-900 text-base mt-2">{r.title}</h4>
              <p className="text-xs text-gray-500 mt-1">{r.desc}</p>
            </div>

            <div className="mt-6 flex items-center justify-between border-t pt-3">
              <span className="text-[11px] text-gray-400 font-medium">Scope: {selectedScope}</span>
              <Button size="xs" variant="primary" onClick={() => handleGenerateReport(r.title)}>
                <Download className="w-3.5 h-3.5 mr-1" /> Export {selectedFormat}
              </Button>
            </div>
          </Card>
        ))}
      </div>

    </div>
  );
};

export default EvaluationReports;
